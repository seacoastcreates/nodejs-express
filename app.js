const { readFile} = require('fs')

const getText = (path) =>
    return new Promise

readFile('./contrent/first.txt', 'utf8', (err, data) {
    if (err) {
        return
    } else {
        console.log(data);
    }
})